<tbody class="b-tbody-data animated fadeIn">
  <% albums.each do |album| %>
    <%= render 'admin/db_list/albums_item', album: album %>
  <% end %>
</tbody>

<% unless albums.last_page? %>
  <%= render 'admin/db_list/albums_paginate' %>
<% end %>
