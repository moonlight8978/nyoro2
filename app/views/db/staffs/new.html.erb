<%= breadcrumb(
  { path: home_path, name: 'ホーム' }, 
  { path: db_path, name: 'データベース' },
  { path: db_albums_path, name: 'アルバムリスト' },
  { path: db_song_path(@song), name: @song.latest_version.title },
  '新しいスタッフを作る'
) %>

<%= page_title("#{@song.latest_version.title}の新しいスタッフをつくる", "new staff for #{@song.latest_version.title_en}") %>

<%= render partial: 'db/staffs/form', locals: {
  url: db_song_staffs_path(@song), method: :post
} %>